{% extends "ISIBundle:Disposition:index.html.twig" %}

{% block title %}
  Dispositions
{% endblock %}


{% block section %}
  <i class="users icon"></i>{{ 'Bâtiments'|trans }}
{% endblock %}


{% block body %}
  <h2 class="ui header">
    <div class="content">
      Bâtiments
      <div class="sub header">Gestion des bâtiments</div>
    </div>
  </h2>
  <div>
    <a href="{{ path('batiment_add', {'as' : asec, 'annexeId': annexe.id}) }}" class="ui primary button">Ajouter un bâtiment</a>
  </div>
  <br>
  <div class="ui cards">
    {% for batiment in batiments %}
      <div class="card">
        <div class="content">
          <div class="header">{{ batiment.nom }}
            <a href="{{ path('batiment_edit', {'as': asec, 'annexeId': annexe.id, 'id': batiment.id}) }}"><i class="ui edit icon"></i></a>
          </div>
          <div class="description">
            Nombre de salle de cours : {{ batiment.salles|length }}
          </div>
        </div>
        <a href="{{ path('salle_add', {'as': asec, 'annexeId': annexe.id, 'id': batiment.id}) }}">
          <div class="ui bottom attached button">
            <i class="add icon"></i>
            Ajouter une salle de cours
          </div>
        </a>
      </div>
    {% endfor %}
  </div>
{% endblock %}


{% block javascript %}
{% endblock %}
